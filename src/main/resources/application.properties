# Temporary fix to allow circular references
spring.main.allow-circular-references=true

# Server Configuration
server.port=8080
server.servlet.context-path=/
spring.application.name=online-quiz-app

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/quiz_app?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=manjula
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Initialize schema and data from SQL files
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html
spring.thymeleaf.cache=false

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=thilagavathi222006@gmail.com
spring.mail.password=twzhxnxjxipmkckt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration
jwt.secret=mySecretKeyForJWTTokenGenerationInOnlineQuizApplication2024EnhancedWithSecurity
jwt.expiration=86400000

# Enhanced logging for debugging
logging.level.com.quizapp=DEBUG
logging.level.com.quizapp.service.UserService=TRACE
logging.level.com.quizapp.service.CustomUserDetailsService=TRACE
logging.level.org.springframework.security=DEBUG

# Fix SecureRandom slow startup
securerandom.strong-algorithm=NativePRNGNonBlocking
securerandom.source=file:/dev/./urandom

# Session management
server.servlet.session.timeout=30m

# Enhanced Quiz Configuration
app.quiz.max-title-length=255
app.quiz.max-description-length=500
app.quiz.max-questions=100
app.quiz.max-options-per-question=6
app.quiz.default-time-limit=30
app.quiz.auto-save-interval=30000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Change from 'always' to 'embedded' or 'never'
spring.sql.init.mode=never

# Or if you want to continue on errors
spring.sql.init.continue-on-error=true


